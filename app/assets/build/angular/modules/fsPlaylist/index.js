'use strict';

module.exports = angular.module('filmsupply.playlist', ['ui.router', 'ngRoute'])
    .config(function($stateProvider, $urlRouterProvider) {
        console.log('Film Supply - Playlist Module loaded')
    });