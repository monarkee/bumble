'use strict';

module.exports = angular.module('filmsupply.player', ['ui.router', 'ngRoute'])
    .config(function($stateProvider, $urlRouterProvider) {
        console.log('Film Supply - Player Module loaded')
    });